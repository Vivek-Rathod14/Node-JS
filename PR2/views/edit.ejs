<!DOCTYPE html>
<html>

<head>
    <title>Edit Task</title>
</head>
<style>
  body {
    font-family: Arial, sans-serif;
    background: linear-gradient(135deg, #667eea, #764ba2);
    min-height: 100vh;
    padding: 40px;
  }

  .container {
    max-width: 600px;
    margin: auto;
  }

  h1 {
    text-align: center;
    color: #fff;
    margin-bottom: 20px;
  }

  form {
    background: #fff;
    padding: 20px;
    border-radius: 12px;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
  }

  input,
  textarea {
    width: 100%;
    padding: 12px;
    margin-bottom: 12px;
    border-radius: 8px;
    border: 1px solid #ccc;
    font-size: 14px;
  }

  button {
    padding: 10px 16px;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    font-weight: bold;
  }

  .add-btn {
    background: #667eea;
    color: #fff;
    width: 100%;
  }

  .todo-list {
    margin-top: 25px;
  }

  .todo-card {
    background: #fff;
    padding: 15px;
    border-radius: 10px;
    margin-bottom: 12px;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
  }

  .todo-card h3 {
    margin: 0;
    color: #333;
  }

  .todo-card p {
    color: #666;
    margin: 6px 0 12px;
  }

  .actions {
    display: flex;
    gap: 10px;
  }

  .edit {
    background: #facc15;
    color: #000;
  }

  .delete {
    background: #ef4444;
    color: #fff;
  }
</style>



<body style="font-family:Arial; background:#f5f5f5; padding:40px;">

    <h1>Edit Task</h1>

    <form action="/edit/<%= todo.id %>" method="POST"
        style="background:#fff; padding:20px; border-radius:10px; width:500px;">
        <input type="text" name="title" value="<%= todo.title %>" required
            style="width:100%; padding:10px; margin-bottom:10px;" />
        <textarea name="description" style="width:100%; padding:10px;"><%= todo.description %></textarea>
        <button type="submit" style="margin-top:10px; padding:10px 20px;">Update Task</button>
    </form>

</body>

</html>